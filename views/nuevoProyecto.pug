extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal 
        h1 #{nombrePagina}

        //- pre= vardump(proyecto)
        //- Validacion
        if errores
            each error in errores
                .alerta.error= error.texto
        //-pasando el proyecto al mixin
        +crearProyecto(proyecto)
            
        
        